export const todoReducer = (state=[], action) =>{

    switch (action.type) {
        // case '':
            
        //     break;
    
        default: //se llama cuando se inicializar
            break;
    }
}